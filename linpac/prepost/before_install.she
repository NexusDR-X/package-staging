#!/usr/bin/env bash
# Runs BEFORE installing package
sudo mkdir -p "/usr/local/share/linpac/macro"
sudo mkdir -p "/usr/local/share/doc/linpac"
sudo mkdir -p "/usr/local/libexec/linpac"
sudo mkdir -p "/usr/local/share/linpac/contrib"
exit 0
